# Copy to .env and fill in. .env is loaded automatically in debug builds
# (cargo run) and is git-ignored. Never commit real secrets.

# Microsoft Graph client secret (value of the secret, not its ID).
CLIENT_SECRET_AZURE=

# SMTP account password, if the SMTP transport is used.
# MAIL_PASSWORD=
