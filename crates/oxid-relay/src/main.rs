//! OxidRelay binary entry point.
//!
//! Skeleton: wires together CLI, configuration, logging, queue, transports and
//! the HTTP API. Implementation will be filled in incrementally.

fn main() -> anyhow::Result<()> {
    println!("OxidRelay - Mail-Relay- und Notification-Service");
    Ok(())
}
